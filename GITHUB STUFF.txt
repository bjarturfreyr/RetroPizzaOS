-- Til að setja changes á github --
git add .
git commit -m "Description"
git push origin master

-- Til að fá changes frá github --
git pull origin master

-- Til að fá original copy af github --
git fetch --all
git reset --hard master